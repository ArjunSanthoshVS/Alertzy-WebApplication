{{!-- <div class="container" style="display: flex;">
  <div class="col-9">
    <div class="container bg-white"
      style="margin-top: 130px; border-radius: 10px; box-shadow: rgb(100 100 111 / 50%) 0px 7px 29px 0px;">

      <body>

        <main>
          <div class="basket mt-5 ">
            <div class="basket-module">

              <h2 class="fw-bold">Your Cart..! <h4>( {{cartCount}} items )</h4>
              </h2>
            </div>
            <div class="basket-labels">
              <ul>
                <li class="item item-heading">Item</li>
                <li class="price">Price</li>
                <li class="quantity">Quantity</li>
              </ul>
            </div>
            {{#if cartCount}}
            {{#each products}}
            <div class="basket-product">
              <div class="item">
                <div class="product-image">
                  <img src="/product-images/{{this.product._id}}/{{this.product._id}}_0.png" alt="Placholder Image 2">
                </div>
                <div class="product-details">
                  <p style="font-weight: 600;">{{this.product.brand}}</p>
                  <h1 style="font-size:27px;"><strong>{{this.product.product}}</strong></h1>
                </div>
              </div>
              <div class="price" style="font-size: 20px; font-weight: 600;">₹{{this.product.offerPrice}}</div>
              <div class="quantity" style=" font-size: 20px; font-weight: 600;">

                <button class="cart-item-count" id="minus{{this.product._id}}"
                  onclick="changeQuantity('{{this._id}}','{{this.product._id}}','{{../userId}}',-1)">-</button>
                <span id="{{this.product._id}}">{{this.quantity}}</span>

                <button class="cart-item-count"
                  onclick="changeQuantity('{{this._id}}','{{this.product._id}}','{{../userId}}',1)">+</button>
                <div class="remove">

                  <button onclick="deleteCart('{{this.product._id}}')" type="button"><i
                      class="fa-solid fa-trash"></i></button>
                  </a>
                </div>
              </div>
            </div>
            {{/each}}
          </div>
          {{else}}
          <img class="" src="/images/empty-cart-2130356-1800917.webp" alt="">
          {{/if}}
        </main>
      </body>
      <div class="ibox-content pb-3">

        <a class=" checkout-cta btn btn-primary" style="width:20%" href="/products"><i
            class="fa fa-arrow-left"></i>Continue to
          shopping</a>


      </div>
    </div>
  </div>

  <div class="col-3 mt-5 my-auto">
    <div class="summary ms-5 mt-5" style=" box-shadow: rgb(100 100 111 / 20%) 0px 0px 9px 0px; margin-top: 50px">
      <div class="summary-total-items"><span class="total-items"></span> Items in your Bag</div>
      {{#each products}}
      <div class="summary-subtotal">
        <div class="subtotal-title w-100">{{this.product.product}}</div>
        <div class="summary-promo hide">
          <div class="promo-title">Promotion</div>
          <div class="promo-value final-value" id="basket-promo"></div>
        </div>
      </div>
      {{/each}}

      <div class="summary-total">
        <div class="total-title">Total</div>
        <div class="total-value final-value" id="basket-total">
          <span id="total">₹ {{totalValue}}</span>
        </div>
      </div>

      <div class="summary-checkout">
        {{#if cartCount}}
        <a href="/place-order" class="checkout-cta btn btn-primary"><b>Place Order</b></a>
        {{else}}
        <h4 class="fw-bold text-danger">Please add Some Products...!</h4>
        {{/if}}
      </div>

    </div>
  </div>
</div> --}}
<section class="h-100 gradient-custom mt-5">
  <div class="container py-5">
    <div class="row d-flex justify-content-center my-4">
      <div class="col-md-8">
        <div class="card mb-4">
          <div class="card-header py-3">
            {{#if cartCount}}
            <h5 class="mb-0">Cart - {{cartCount}} items</h5>
            {{else}}
            <h5 class="mb-0">Cart - 0 items</h5>
            {{/if}}
          </div>
          {{#if cartCount}}
          {{#each products}}
          <div class="card-body">
            <!-- Single item -->
            <div class="row">
              <div class="col-lg-3 col-md-12 mb-4 mb-lg-0">
                <!-- Image -->
                <div class="bg-image hover-overlay hover-zoom ripple rounded" data-mdb-ripple-color="light">
                  <img src="{{this.product.image.[0]}}" class="w-100" alt="Blue Jeans Jacket" />
                  <a href="#!">
                    <div class="mask" style="background-color: rgba(251, 251, 251, 0.2)"></div>
                  </a>
                </div>
                <!-- Image -->
              </div>

              <div class="col-lg-5 col-md-6 mb-4 mb-lg-0">
                <!-- Data -->
                <p><strong>{{this.product.brand}}</strong></p>
                <h5><strong>{{this.product.product}}</strong></h5>

                <button onclick="deleteCart('{{this.product._id}}')" type="button"
                  class="btn btn-primary btn-sm me-1 mt-4" data-mdb-toggle="tooltip" title="Remove item">
                  <i class="fas fa-trash"></i>
                </button>
                <button type="button" class="btn btn-danger btn-sm mt-4" data-mdb-toggle="tooltip"
                  title="Move to the wish list">
                  <i class="fas fa-heart"></i>
                </button>
                <!-- Data -->
              </div>

              <div class="col-lg-4 col-md-6 mb-4 mb-lg-0">
                <!-- Quantity -->
                <div class="d-flex mb-4" style="max-width: 300px">
                  <button type="button" class="btn btn-primary me-2 cart-item-count" id="minus{{this.product._id}}"
                    onclick="changeQuantity('{{this._id}}','{{this.product._id}}','{{../userId}}','{{this.product.stock}}',-1)">
                    <i class="fas fa-minus"></i>
                  </button>

                  <div class="form-outline" style="font-size:24px;">
                    <span id="{{this.product._id}}">{{this.quantity}}</span>
                  </div>

                  <button type="button" class="btn btn-primary  ms-2 cart-item-count" id="plus{{this.product._id}}"
                    onclick=" changeQuantity('{{this._id}}','{{this.product._id}}','{{../userId}}','{{this.product.stock}}',1)">
                    <i class="fas fa-plus"></i>
                  </button>
                </div>
                <!-- Quantity -->

                <!-- Price -->
                <h4 class="text-start text-md-center ">
                  <strong>₹{{this.product.offerPrice}}</strong>
                </h4>
                <!-- Price -->
              </div>
            </div>
            <!-- Single item -->

            <hr class="my-4" />

            <!-- Single item -->
          </div>
          {{/each}}
          {{else}}
          <img class="" src="/images/empty-cart-2130356-1800917.webp" alt="">
          {{/if}}
        </div>
        {{!-- <div class="card mb-4">
          <div class="card-body">
            <p><strong>Expected shipping delivery</strong></p>
            <p class="mb-0">12.10.2020 - 14.10.2020</p>
          </div>
        </div> --}}
        <div class="card mb-4 mb-lg-0">
          <div class="card-body">
            <p><strong>We accept</strong></p>
            <img class="me-2" width="45px"
              src="https://mdbcdn.b-cdn.net/wp-content/plugins/woocommerce-gateway-stripe/assets/images/visa.svg"
              alt="Visa" />
            <img class="me-2" width="45px"
              src="https://mdbcdn.b-cdn.net/wp-content/plugins/woocommerce-gateway-stripe/assets/images/amex.svg"
              alt="American Express" />
            <img class="me-2" width="45px"
              src="https://mdbcdn.b-cdn.net/wp-content/plugins/woocommerce-gateway-stripe/assets/images/mastercard.svg"
              alt="Mastercard" />
          </div>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card mb-4">
          <div class="card-header py-3">
            <h5 class="mb-0">Summary</h5>
          </div>
          <div class="card-body">
            {{#each products}}
            <div class="summary-subtotal">
              <div class="d-flex">
                <img style="width:100px;" src="{{this.product.image.[0]}}" alt="Placholder Image 2">

                <div class="subtotal-title w-100 my-auto">
                  <h5>{{this.product.product}}</h5>
                </div>
              </div>
              <div class="summary-promo hide">
                <hr>
                <div class="promo-value final-value" id="basket-promo"></div>
              </div>
            </div>
            {{/each}}
            {{#if cartCount}}
            <ul class="list-group list-group-flush">
              {{!-- <li class="list-group-item d-flex justify-content-between align-items-center border-0 px-0 pb-0">
                Total
                <span>₹<span id="total">{{totalValue}}</span></span>
              </li> --}}
              {{!-- <li class="list-group-item d-flex justify-content-between align-items-center px-0">
                Shipping
                <span style="color:green;">Free</span>
              </li> --}}

              <li class="list-group-item d-flex justify-content-between align-items-center border-0 px-0 mb-3">
                <div>
                  <strong>Total amount</strong>
                  <strong>
                    <p class="mb-0">(including GST)</p>
                  </strong>
                </div>
                <span>₹<strong><span id="total">{{totalValue}}</span></strong></span>
              </li>
            </ul>
            <a href="/place-order">
              <button type="button" class="btn btn-primary btn-block">
                Go to checkout
              </button></a>
            {{else}}
            <ul class="list-group list-group-flush">
              {{!-- <li class="list-group-item d-flex justify-content-between align-items-center border-0 px-0 pb-0">
                Total
                <span>₹<span id="total">{{totalValue}}</span></span>
              </li> --}}
              {{!-- <li class="list-group-item d-flex justify-content-between align-items-center px-0">
                Shipping
                <span style="color:green;">Free</span>
              </li> --}}

              <li class="list-group-item d-flex justify-content-between align-items-center border-0 px-0 mb-3">
                <div>
                  <strong>Total amount</strong>
                  <strong>
                    <p class="mb-0">(including GST)</p>
                  </strong>
                </div>
                <span>₹<strong><span id="total">0</span></strong></span>
              </li>
            </ul>
            <a href="/products">
              <button type="button" class="btn btn-primary btn-block">
                Explore Products
              </button></a>
            {{/if}}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  function changeQuantity(cartId, prodId, userId, stock, count) {
    let quantity = parseInt(document.getElementById(prodId).innerHTML)
    count = parseInt(count)
    console.log(quantity);

    quantityCheck = quantity + count
    console.log(quantityCheck);
    stock = parseInt(stock)
    if (quantityCheck <= stock && quantityCheck != 0) {
      document.getElementById("minus" + prodId).classList.remove("invisible")
      document.getElementById("plus" + prodId).classList.remove("invisible")
      $.ajax({
        url: '/change-product-quantity',
        data: {
          user: userId,
          cart: cartId,
          product: prodId,
          count: count,
          quantity: quantity
        },
        method: 'post',
        success: (response) => {
          console.log(response);
          if (response.removeProduct) {
            location.reload()
          } else {
            document.getElementById(prodId).innerHTML = quantity + count;
            document.getElementById('total').innerHTML = response.total
          }
        }
      })
    }
    if (quantityCheck == 1) {
      document.getElementById("minus" + prodId).classList.add("invisible")
    }
    if (quantityCheck == stock) {
      document.getElementById("plus" + prodId).classList.add("invisible")
    }
  }


  function deleteCart(prodId) {
    swal({
      title: "Are you sure?",
      type: "warning",
      showCancelButton: true,
      confirmButtonColor: "red",
      confirmButtonText: "Yes",
      cancelButtonText: "No",
      closeOnConfirm: false,
      closeOnCancel: true
    },
      function (isConfirm) {
        if (isConfirm) {
          $.ajax({
            url: '/delete-cart-product/' + prodId,
            method: 'get',
            success: (response) => {
              if (response) {
                location.reload()
              }
            }
          })
        }
      }
    );
  }
</script>